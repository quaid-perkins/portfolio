import { Component } from "solid-js";

const Header: Component = () => {
	return (
		<header class="inline-flex justify-center sticky top-0 w-full py-4 bg-white/80 border-b border-zinc-100/80 backdrop-blur-sm ">
			<nav></nav>
		</header>
	);
};

export default Header;
